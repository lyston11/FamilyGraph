# 父任务总验收报告（2026-09-19）

对照父任务 PRD §6 的 AC-01～09 逐项判定。**已解决/未解决分别写明，不用另一子任务的成功代替**。

基线：`main@05fa886`；生产运行版本经 G-2 部署核对为 `33d8be6` 代码 + 重建 dist。
子任务状态：A/B（归档，前期实现）、C/D/E/F/G/I 已归档，09-18 已归档，D2/G2 已归档。

## 逐项判定

| AC | 判定 | 依据与证据 | 仍未解决的边界 |
| --- | --- | --- | --- |
| **AC-01** 两条链路阶段定义、样本窗、部署 commit、缺失标记与失败分母 | **已闭合** | D：`assistant_phases` 带 `basis`/`native_n`/`derived_n`，分母由 run 表 LEFT JOIN（`runs_without_events`/`without_first_lease`/`without_start`）；`timing_json`（迁移 0051）为源计时。管家侧 B1–B5 用 fake transport 证明程序合同。 | 历史行 `timing_json` 为 NULL 按 unknown 处理（设计如此，非缺口）。 |
| **AC-02** 第一笔合法返回不被第二笔慢请求退回 unknown | **已闭合** | 09-17 B/C：逐笔结算（返回即在其执行身份仍有效时提交）、混合批次独立产物可消费、结算预留与出事务再核对。B 组 4+10 tests。 | fake transport 证明程序合同，不证明真实上游时延分布。 |
| **AC-03** 混合批次安全恢复；撤权/证据变化拒绝；重复 recovery 无重复计费 | **已闭合** | B3/B4：失租接管/中断零业务写回，崩溃点恢复（发前/发后未审计/写回前）。 | 同上。 |
| **AC-04** 慢分块/发送前耗尽/失租/中断的时限与资源有界；旧执行者零写回 | **已闭合** | C：`asyncio.timeout` 整笔墙钟截止（B1c 在**真实 HTTP transport 层**测量）；F 受控矩阵 B1/B1b/B1c 复验；F 的 A6 取消收敛 + `fence_execution` 成员资格 403。 | — |
| **AC-05** unknown 不重发、未发送有限重试、预算上限、四类辅助校验 | **已闭合** | E：两层预算显式冻结（`SESSION_RETRY_BUDGET` 防 SDK 漂移）；永久 4xx 恰好 1 次出站；unknown 不自动重发；B5 预算用例 5 passed。 | **降低总预算未批准**（E-R3/E-R5），当前只冻结不降额——这是策略选择，不是验收缺口。 |
| **AC-06** 可解释主导等待；程序性优化证明阶段减少且语义不变；展示改进不计推理提速 | **已闭合** | F 矩阵 A1-4（源计时 vs 持久间隔）、A1-5（注入延迟归到正确阶段）、A3-1/A3-4（压缩 run 级归属）；I 决策台账区分「首段更早/完整更快/吞吐」三目标并给出维持建议。上游 `header_ms` 1032–7844ms 是主导项，如实归因。 | 真实端到端用户可见时长未有本版本测量；I 已写明最小补证路径。 |
| **AC-07** 真实验证按批准 Provider + 合成小样本，记录可达/调用/合法输出/应用；不宣称稳定 p95 | **已闭合** | G-3：真实 Provider 4/4 成功，逐笔记录 `sent`/`upstream_status`/字节/时长；五段判定分列；不报告 token 精确值（上游未返回 usage）。生产库未污染。 | steward 真实样本未运行（开关默认关 + 归档扫描已证无可改善候选），如实保留。 |
| **AC-08** 受影响包门禁、API smoke、浏览器验证；无凭据/正文泄露；迁移隔离演练 | **已闭合** | F：backend 1716 passed、agent 167、frontend 767、lint/type-check/build 全绿；smoke 56/56 退出 0；真实浏览器 22/22（含失权 3 格）。G-2：发布前在线备份 + integrity + 行数；报告无凭据/正文。 | 家庭前端制品不在远端（本地 Vite 提供），已在 G-2 说明。 |
| **AC-09** 总验收报告逐项；Spec/HANDOFF 同步；串行集成、归档前验收全满足并清理 worktree | **已闭合** | 本文件即总验收；Spec 已更新（压缩归属、取消失效、settle 精度、reaper 非可重试条件）；HANDOFF 已同步；各任务串行集成、worktree 与分支已清理。 | — |

## 用户慢响应：状态与归因（不得用子任务成功代替）

用户最初的问题是「助手响应 10–30s」。分三层如实回答：

| 层面 | 状态 | 证据 |
| --- | --- | --- |
| **已生成但不可见** | **已解决** | 09-18 增量显示 P0-2 部署；F 浏览器格 UI1-2/1-3/1-4 证明增量为先、权威整体替换；实测该等待从整条生成时长降到 151–491ms |
| **排队等待** | **部分改善，有界** | 09-18 P0-1 轮询 2000→250ms 已部署；I 探针实测单 worker 串行且排队占比 55–71%（受控注入下）；账户并发上限=2 是硬约束 |
| **上游推理/首响应头** | **未解决（属上游）** | G-3 实测上游 `header_ms` 3.5–11.7s；09-18 实测 1032–7844ms。这是外部依赖，本项目不能通过调整自身参数消除 |

**因此不宣布「3s 必达」或任何百分比提速。** 已在可控层面（可见性、轮询、预算、取消语义）
取得可证改善；剩余等待经证据归因到上游，AC-06 允许在此情形下交付可信归因与可审阅方案。

## 未闭合项（如实列出，不阻塞父任务）

| 项 | 状态 | 说明 |
| --- | --- | --- |
| 降低总重试预算 | 待用户批准 | E-R3/E-R5 的数值选择；当前只冻结不降额 |
| steward 真实模型改善 | 未达成（非缺陷） | 归档扫描证明当前词表下无「改善」候选可判定 |
| 真实端到端用户可见时长（本版本） | 未测 | I 已给最小补证路径（读 latency 端点 `queue_wait` 分布、复用 09-18 浏览器探针） |
| 跨机时钟精度 | 未测 | 需另建探针；当前所有计时为本机 monotonic |
| `expired` 前端专用文案 | 未做 | 不在缺陷路径上，已记录 |
| 撤权后历史读取授权语义 | 现状按账号归属放行 | 是否收紧为逐事件空间复核属独立设计决策 |

## 归档前检查

- 各子任务分支已合并进 main 且已 push（`origin/main = 05fa886`）。
- worktree 与本地分支已清理，无残留（除本父任务自身的隔离 worktree）。
- 迁移单头 `0051_run_event_timing`，生产库与代码一致。
- 生产服务运行版本已核对（PID/启动时间/制品内容三者一致）。
