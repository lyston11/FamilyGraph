# V2.6 注记

- 联网原始需求为“暂定”，因此作为最后、默认关闭、可独立延后的阶段。
- Agent 不直接拥有 HTTP；受控搜索/fetch 仍是 FastAPI 领域工具。
- 用户要求联网不意味着可把亲属 PII 发往外部；平台红线优先。
- 外部网页只能作为不可信资料，不能自动变 SourceFact 或 Steward 推荐依据。
- 发布验收包含 Agent/Memory/RAG 数据恢复，但当前无真实数据迁移。
