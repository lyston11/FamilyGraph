# V2.6 交接

## 交付目标

在默认关闭和严格 egress/PII/引用边界下启用 Web search/fetch，并完成整个 v2 Agent 系统的发布与恢复门禁。

## 完成出口

SSRF、prompt injection、隐私最小化、配额、kill switch、Compose、SSE、backup/restore 和全量 E2E 全绿。

## 后继

本任务完成即进入父任务跨阶段集成复审；MatchBroker、多空间 Session、任意 MCP/浏览器仍需另开 v2.x 规划。
