# V2.4 注记

- Steward 负责“正式信息与投影的维护编排”，不负责把所有内容变成 Memory。
- 用户称谓修改等真实行为直接进 DomainEvent；这回答了“很多用户行为确实要收集”的要求。
- “推荐卡片”不是自动操作；特别是加入申请，必须由用户再次触发发送。
- 被创建人物确档前没有推荐，即使 provisional node 已在空间图中可见。
- 朋友不参与家谱推荐；guest 是用户手工空间邀请，不是亲属推断。
