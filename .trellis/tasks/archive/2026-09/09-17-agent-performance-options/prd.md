# I：双 Agent 模型档位、排队与性能取舍评估

## 状态与目标

planning，**评估与决策任务**，本次不调参、不换模型、不扩容、不发真实模型请求。主会话内联。父任务 `09-17-dual-agent-latency-result-integrity`。

目标：基于可信阶段证据，分别评估推理档位/模型、排队/并发、重试等待和预算，不将质量下降或调用费用增加藏在“变快”里。

## 当前事实与未知

项目SDK探针支持默认thinkingLevel为medium；平台支持档位列表不等于实际请求档位。当前Provider/协议/模型选择与CCSwitch/Pi来源必须按执行时实际解析链复核。历史只有2个助手run，旧queue/tool指标受入库时间影响，不能宣称并发永无价值或降档必然提速。单sidecar串行是事实，是否主导等待需D/F/G证据。

## 需求与验收

| ID | 需求 | 完成判据 |
| --- | --- | --- |
| I-R1/AC1 | 同口径性能基线 | 引用D/F/G的样本版本、来源、失败分母、缺失值，区分排队、生成/重试、首可见与总时长；不足以归因就给不足结论。 |
| I-R2/AC2 | 质量/费用不隐含牺牲 | 每个选项列预期收益、证据强度、答案/工具/引用风险、token/请求费用、适用场景和停止条件。 |
| I-R3/AC3 | 模型与档位准入 | 现有provider profile门禁保持；新档位/模型只是待选择方案，先给合成代表性质量集和有界A/B设计，不能擅自绕过支持列表。 |
| I-R4/AC4 | 并发与排队评估 | 本地零费用多run模拟测单worker排队、轮询、上下文、SQLite竞争和限流影响；给扩容前提，不把提高吞吐当缩短单次生成。 |
| I-R5/AC5 | 策略与可回退性 | 与E重试治理保持唯一策略来源；每项给配置入口/发布回退和所需授权，不复制一套retry规则。 |
| I-R6/AC6 | 明确决策记录 | 记录维持/采用/延期/否决及理由、剩余未知；未有真实A/B不得宣称质量无损或稳定p95。 |

## 依赖与非目标

D/F可信计时、E重试预算、G同配置真实验证作为证据输入；可先列评估矩阵，最终结论等待数据。增量显示优化已归 09-18（原 H 已归档），单独报告，无需捆绑降档。C～G可以在维持现配置下完成，不因I无优化结论而强制换模型。

评估可能合法得出“维持当前配置，样本不足暂不采用”。正式调参实现、生产扩容或真实A/B需后续明确批准，并补实现AC；此任务不负责实际上线这些选择。不得用recent-N截断/关闭自动压缩、绕过权限或增加无限重试来做基线。
