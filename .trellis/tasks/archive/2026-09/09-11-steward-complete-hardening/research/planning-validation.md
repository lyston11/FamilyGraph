# 规划工件校验记录（2026-09-11）

本记录验证九个 Steward 任务的规划资料及引用完整性，不代表产品修复已完成。校验范围为一个父任务、六个修复/发布子任务、两个延期研究子任务；全部 task.json 保持 planning，实施检查单未勾选。

## Trellis 原生校验

对这九个任务逐一执行 `python3 .trellis/scripts/task.py validate <task-dir>`，退出码全部为 0，没有上下文截断或代码文件注入警告。18 个 JSONL 文件共 84 条真实 spec/research 引用；无示例占位行。此命令只验证上下文文件，内容与依赖另行检查。

## 文档与任务结构检查

本轮一次性只读校验脚本检查并通过以下项目：

- 每个任务都有 task.json、prd.md、design.md、implement.md、notes.md、implement.jsonl、check.jsonl。
- 父任务 children 与八个子任务 parent 双向一致，依赖图无环，延期任务有明确研究准入条件。
- 43 个需求编号均有对应的可观察验收项；23 条事项编号唯一，每条都有主责任务及有效 AC 编号。
- 58 处 relatedFiles 路径真实存在；拟新增路由、模型、迁移和测试的位置在 design/implement 中注明。
- Markdown 相对链接及锚点有效，代码围栏闭合，无模板占位、尾随空白或缺少结束换行；检查包含尚未被 Git 跟踪的新文件。
- task title 与 PRD 标题一致；没有把延期研究、潜在风险或规划文档标作已完成的产品能力。

主线程已逐份审阅最终 PRD，并核对跨任务合同：网络请求退出写事务、预算不确定结果处置、全路径与 ETag 授权重验、候选受众隔离、当事人确认、通知已读无领域副作用。个人词偏好与关系提案分别使用各自命令和返回状态，未知/隐藏建议统一 404。

## 本轮未执行的验证

没有运行产品测试、迁移、服务启动、真实 provider 调用或用户数据操作。09-09 报告的核心/维护 46 passed 与辅助 16 passed 是历史证据，不能作为这批未来修复通过的证明。实现阶段应按每个 implement.md，在隔离 DATA_DIR 中执行回归与故障注入，并把实际结果写入 notes。

本轮仅写入这九个任务目录；工作区已有空间关联、前端及 conftest 等改动没有由本轮覆盖或提交。正式实现时须重新检查现行代码、用户并行改动及 Alembic head。
