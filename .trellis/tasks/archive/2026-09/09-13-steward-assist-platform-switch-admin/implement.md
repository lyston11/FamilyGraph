# 2026-09-14 补充修复执行计划

1. 复用既有任务的活动目录建立隔离 worktree，原活动副本、正式归档和指向旧副本的 runtime 会话先保全。
2. 实施子代理只改平台能力组件与对应测试；先以组件测试复现返回关闭但提示启用，再完成状态、来源及可操作性修复。
3. 检查子代理独立审阅两个业务文件与追加验收要求；运行或核对管理员前端完整质量门禁。主代理抽查关键结论。
4. 主代理保留原 2026-09-13 验收与完成日期，在原归档追加当前结果与修复证据；串行提交、合并 main、push。
5. 同名归档已存在，task.py archive 不支持覆盖；以既有归档为唯一正式记录，使用 Trellis 元数据及会话清理辅助函数对账，备份移走活动目录。归档后立即删除已合并且无未提交代码的 worktree/本地分支，登记 journal。

规范判断：`.trellis/spec/` 索引明确标为历史资料，遵从当前 AGENTS.md、实际组件/API 合同及本 PRD。此次学到的生效状态提示规则追加至正式归档，避免把历史 spec 当作现行门禁。

验证范围：只改管理员前端，不重复跑 backend/家庭端全量测试，不改线上开关或部署。现有 API 类型和后端 env 与 DB 生效规则作为只读依据。
