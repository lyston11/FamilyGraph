# M4 技术设计（父级）

权威契约：architecture.md §8（备份恢复）。里程碑级决策：

- 备份实现为 api 镜像内 CLI 入口而非独立服务（几十人规模无定时需求时手动执行即可；如需 cron 由宿主机 crontab 调 docker exec，不引入新容器）。
- 回归清单以文件形式维护在 m4c 任务目录（checklist.md），每项链接到对应子任务验收标准，作为发布唯一依据。
